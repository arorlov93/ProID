# Отчёт по самопроверке

Все числа — вывод скриптов из `scripts/`, а не оценка на глаз. Проверки
воспроизводятся: `npm run build && npm run preview`, затем `node
scripts/audit.mjs`, `node scripts/checks.mjs`, `node scripts/shots.mjs`.

Дата прогона: 16 августа 2026. Сборка: 24 страницы, 119 499 знаков без пробелов.

---

## 17 пунктов самопроверки

| № | Проверка | Норма | Результат | |
|---|---|---|---|---|
| 1 | Текст виден без JS | найден на всех | **18 из 18** страниц, 0 скрытых блоков на 19 страницах | ✅ |
| 2 | Lighthouse Performance, мобильные | ≥ 80 | **100** на 4 страницах | ✅ |
| 3 | Lighthouse Performance, десктоп | ≥ 90 | **100** на 4 страницах | ✅ |
| 4 | Lighthouse SEO и Accessibility | ≥ 95 | **100 / 100** на 4 страницах | ✅ |
| 5 | LCP мобильные | ≤ 2,0 с | **1,1 с** (локально, см. оговорку) | ⚠️ |
| 6 | CLS | ≤ 0,1 | **0** на всех проверенных | ✅ |
| 7 | Вес страницы | ≤ 1,2 МБ | **91–260 КБ** без сжатия, 4–5 запросов | ✅ |
| 8 | Валидность HTML | без критических ошибок | **0 ошибок**, `html-validate` по 24 страницам | ✅ |
| 9 | Микроразметка | без ошибок | **0 ошибок разбора**, 8 типов, 122 блока — но валидаторами Яндекса и Google не проверена | ⚠️ |
| 10 | Ширина 320 px | нет горизонтального скролла | **0 переполнений** на 19 страницах | ✅ |
| 11 | Скриншоты 1440 и 390 | приложены, просмотрены | **38 файлов** в `shots/`, разбор ниже | ✅ |
| 12 | Контраст текста | ≥ 4,5:1 | **0 нарушений** на 5 страницах | ✅ |
| 13 | Клавиатура, видимый фокус | работает | **22 элемента, 0 без фокуса**, первый — «Перейти к содержанию» | ✅ |
| 14 | Формы отправляются | заглушка работает, точка помечена | валидация, маска, honeypot, благодарность — работают | ✅ |
| 15 | Битые ссылки | нет | **0 из 1 359** внутренних ссылок | ✅ |
| 16 | Запрещённые обороты | не найдены | **0 из 9** проверяемых | ✅ |
| 17 | Неразрывные пробелы и тире | расставлены | 6 331 / 586 / 336, прямых кавычек 0 | ✅ |

Два пункта помечены ⚠️ не потому, что провалены, а потому что измерены не в тех
условиях, в которых их будут принимать. Подробности ниже.

---

## Что означают числа

### 1. Текст без JavaScript

Проверка ищет содержательные фрагменты в готовом HTML, вырезав всё внутри
`<script>` (иначе засчитался бы JSON-LD). Найдено на 18 из 18 проверяемых
страниц.

Отдельно проверено с выключенным JavaScript в браузере: на 19 страницах **0
блоков остались скрытыми**, на главной видно 11 006 знаков текста и 8 чертежей.
Это работает потому, что скрытое начальное состояние блоков появления задано
селектором `.js [data-reveal]`, а класс `js` ставит встроенный скрипт до первой
отрисовки.

### 2–5. Скорость

```
                             perf  a11y  seo  bp  | LCP    CLS  TBT
/                    моб.    100   100   100  100 | 1,1 с  0    0 мс
/ceny                моб.    100   100   100  100 | 1,1 с  0    0 мс
/oformlenie-id       моб.    100   100   100  100 | 1,1 с  0    0 мс
/blog/prichiny-...   моб.    100   100   100  100 | 1,1 с  0    0 мс
/                    десктоп 100   100   100  100 | 0,3 с  0    0 мс
/ceny                десктоп 100   100   100  100 | 0,3 с  0    0 мс
/oformlenie-id       десктоп 100   100   100  100 | 0,3 с  0    0 мс
/blog/prichiny-...   десктоп 100   100   100  100 | 0,3 с  0    0 мс
```

**Оговорка, без которой числа врут.** Замер сделан против локального сервера:
нулевая сетевая задержка, без TLS-рукопожатия, без сжатия. Троттлинг Lighthouse
эмулирует медленную сеть, но не заменяет реальный канал и реальный хостинг.

Чего ждать в бою: при gzip и обычном российском хостинге LCP на мобильных
разумно ожидать в диапазоне 1,2–1,8 с — то есть норма ТЗ (≤ 2,0 с) достижима,
но подтвердить её можно только на PageSpeed Insights после выкладки. **Это надо
перемерить в день запуска.**

Что уже сделано в пользу LCP: статика без клиентского рендеринга, системный
шрифт (ноль запросов на шрифты), критический CSS инлайном, ноль внешних
библиотек, ноль растровых изображений в вёрстке — вся графика векторная и
встроена в разметку.

### 6. CLS = 0

Ноль на всех замерах. Достигается тем, что у чертежей задано `aspect-ratio`,
шрифт системный (нет подмены), а блоки появления меняют только `opacity` и
`transform` — эти свойства не вызывают перекомпоновку.

### 7. Вес и запросы

| Страница | Вес без сжатия | Запросов |
|---|---|---|
| `/` | 260 КБ | 5 |
| `/ceny` | 123 КБ | 5 |
| `/oformlenie-id` | 111 КБ | 4 |
| `/blog/prichiny-vozvrata-id` | 98 КБ | 4 |
| `/checklist` | 91 КБ | 4 |

Норма ТЗ — 1,2 МБ и 40 запросов. Запас пятикратный по весу и восьмикратный по
запросам. При gzip на сервере вес упадёт примерно вчетверо.

Главная тяжелее остальных: 14 блоков, восемь инженерных чертежей, калькулятор,
полный прайс на два раздела и четыре формы на одной странице. Чертежи — векторные
и встроены в разметку, поэтому дают вес HTML, но ни одного запроса.

### 8. Валидность HTML

`html-validate` с набором `recommended` по всем 24 страницам: **0 ошибок,
0 предупреждений**.

По пути найдено и исправлено 12 ошибок: заголовки статьи начинаются с номера,
и генератор якорей делал идентификаторы вида `id="1-nepolnaya-opis"`. Такой
идентификатор не выбирается селектором CSS. Исправление — в
`src/integrations/typograf.js`, переписываются и `id`, и ссылки на них.

Отдельно: `html-validate` — не сервис W3C. Правила совпадают почти полностью,
но **прогнать через validator.w3.org после выкладки всё же стоит** — из
окружения он недоступен.

### 9. Микроразметка

122 блока JSON-LD на 24 страницах, **0 ошибок разбора**:

| Тип | Страниц |
|---|---|
| `Organization` | 24 |
| `ProfessionalService` (LocalBusiness) | 24 |
| `WebSite` | 24 |
| `BreadcrumbList` | 22 |
| `FAQPage` | 14 |
| `Service` | 7 |
| `ItemList` | 4 |
| `Article` | 3 |

Разметка связана через `@id`: `Service` ссылается на `Organization` как
поставщика, `ProfessionalService` — на неё же как на родительскую организацию.

**Чего не сделано.** Валидатор Яндекса и Rich Results Test из окружения
недоступны — сеть закрыта. Проверено локально: JSON корректен, обязательные
поля типов заполнены, ссылки между узлами разрешаются. Прогнать через оба
сервиса после выкладки — обязательно, это пункт 4 критериев приёмки ТЗ.

### 10. Узкие экраны

19 страниц на ширине 320 px, с полной прокруткой каждой: **0 горизонтальных
переполнений**.

По пути найдено и исправлено: сетка-подложка чертежей выходила за `viewBox` и
при `overflow: visible` расширяла документ на 7 px. Теперь обрезается по рамке
элемента.

Цели нажатия на мобильных: **136 элементов, 0 ниже 44 px по высоте**. Исправлено
два места — ссылка-логотип в шапке (была 34 px) и ползунки калькулятора (дорожка
3 px). У ползунка дорожка осталась тонкой визуально, а область нажатия выросла
до 44 px через псевдоэлемент.

Единственное принятое исключение: флажок согласия 20×20 px. Он обёрнут в
`<label>`, который кликается целиком и заведомо выше 44 px, — это допустимо по
WCAG 2.5.8.

### 12. Контраст

**0 нарушений** на 5 страницах после исправлений. Проверка обходит все текстовые
элементы, вычисляет фон с учётом наложения полупрозрачных слоёв и считает
отношение яркостей по WCAG.

Найдено и исправлено реальное нарушение: `--tx-l-dim` (#6b7482) давал 4,6:1 к
основному светлому фону, но всего **3,9:1** к более тёмной светлой поверхности
`--l3` — на ней стоит правая колонка калькулятора. Обе роли текста на светлом
пересчитаны под самый тёмный из светлых фонов: `--tx-l-mut` → #565f6b,
`--tx-l-dim` → #5f6875.

Заодно исправлена ошибка в самой проверке: она читала `rgba(224,169,74,0.07)`
как непрозрачный янтарь и выдавала ложные 1:1 и 2,8:1. Теперь слои смешиваются.

### 13. Клавиатура

22 элемента обхода на главной, **0 без видимого фокуса**. Первым в порядке
обхода идёт ссылка «Перейти к содержанию» — она появляется при фокусе и
пропускает шапку.

Обводка фокуса задана глобально на `:focus-visible` и на светлых секциях
переключается на тёмный оттенок янтаря, чтобы не терялась.

### 14. Формы

Приёмник не подключён — токена Telegram-бота нет. Проверено то, что работает
сейчас:

| Проверка | Результат |
|---|---|
| Пустая отправка | 2 сообщения об ошибке, фокус уходит в первое поле |
| Маска телефона | `9161234567` → `+7 916 123-45-67` |
| Экран благодарности | показан без перезагрузки страницы |
| Honeypot заполнен | 0 исходящих запросов |
| Ограничение частоты | 20 с между отправками |

Точка подключения помечена `TODO(разработчик)` в `src/scripts/ui.ts`. Формат
данных и требования к приёмнику — в `README.md`, раздел «Подключение форм».

### 16–17. Тексты и типографика

Запрещённые обороты: **0 совпадений** из 9 проверяемых («динамично
развивающаяся», «индивидуальный подход», «команда профессионалов», «широкий
спектр», «гибкая система скидок», «качественно и в срок», «на рынке с»,
«высококвалифицированные», «под ключ по доступным»).

Типографика по всем 24 страницам:

| Что | Сколько |
|---|---|
| Неразрывные пробелы | 6 331 |
| Длинные тире — | 586 |
| Средние тире – в диапазонах | 336 |
| Кавычки-ёлочки | 156 |
| Прямые кавычки в тексте | 0 |
| Дефисы вместо тире (« - ») | 0 |

Обработано 2 311 текстовых узлов. Атрибуты не тронуты намеренно: неразрывный
пробел в `title` или `description` портит сниппет выдачи.

### Объём контента

119 499 знаков без пробелов на 24 страницах.

| Тип страницы | Норма ТЗ | Факт |
|---|---|---|
| Посадочные услуг (7) | 2 500–4 000 | 6 231–7 034 |
| Статьи блога (3) | 4 000–7 000 | 6 446–8 717 |
| Карточки объектов (4) | 1 200–2 000 | 2 348–2 543 |

Все выше нижней границы. Посадочные вышли за верхнюю — это осознанно: объём
набран не водой, а блоками «что входит» (10 позиций), «когда это нужно», FAQ на
6 вопросов и перелинковкой, которых требует раздел 3 ТЗ. Сокращать пришлось бы
за счёт этих блоков.

---

## Скриншоты: что хорошо и что слабо

38 файлов в `shots/` — 19 страниц на 1440 и 390 px (сжаты в JPEG, чтобы не
раздувать репозиторий; полноразмерные PNG пересоздаются `node
scripts/shots.mjs`). Просмотрены все. Честно по каждой:

**Главная.** Первый экран собран: заголовок с акцентом на «с первого раза»,
две кнопки в строку, плашка «отвечаем в течение часа», четыре цифры, план этажа
справа. План построен по правилам черчения: стены в поше с пробитыми проёмами,
окна с переплётом, двери с дугами открывания, лестница с проступями, стрелкой
подъёма и косой линией обрыва, три размерные цепочки, оси в кружках, отметка
уровня, марки разреза, многослойная выноска на состав стены и основная надпись
формы 1. Размерные числа считаются из геометрии, поэтому цепочки сходятся с
габаритом 24 000.
*Слабое:* правая колонка заканчивается заметно выше левой, под чертежом остаётся
пустота — на широких экранах это видно. Страница длинная, 15 000 px: 14 блоков
ТЗ на одном полотне — много, часть посетителей до FAQ не дойдёт.

**Задачи, услуги, УТП.** Карточки ровные, иконки в едином штрихе 1,4 px.
*Слабое:* иконка аудита (лупа над листом) в 22 px читается мутно — при 24 px
было бы честнее, но тогда ломается ритм плашек.

**Калькулятор.** Лучший блок страницы: ввод слева, расчёт справа, вилка и срок
крупно, разбивка по строкам. Пересчёт мгновенный, значение дублируется полем и
ползунком.
*Слабое:* на 900–1000 px правая колонка сжимается и подпись срока переносится
под сумму — работает, но выглядит теснее, чем на 1440.

**Прайс.** Таблицы с моноширинными цифрами, подсветка строки при наведении, ниже
640 px перестраивается в карточки.
*Слабое:* 28 позиций подряд — длинная простыня. Группировка по пяти разделам
помогает, но якорного меню по разделам нет.

**Посадочные услуг.** Одинаковый каркас: первый экран с ценой, форма сразу после
него, «что входит», «когда это нужно», отличие, текст, FAQ, перелинковка, форма
в конце. Титульный лист тома в шапке выглядит убедительно после того, как штамп
приёмки перестал наезжать на опись.
*Слабое:* семь страниц по одному шаблону — при переходе между ними ощущение
однообразия. Различают их только чертёж в шапке и содержание.

**Объекты.** Плитки 2×2, чертёж-подложка растворяется к краю карточки маской —
без неё штамп резался посреди слова. Подложки разные по типу листа: план,
разрез, титул, узел.
*Слабое:* четыре карточки вместо пяти, и в паспортах нет цифр. Это честно (см.
`DECISIONS.md`, п. 5), но раздел выглядит скромнее остальных.

**Блог.** Статья читается хорошо: узкая колонка, блок «Коротко» перед текстом,
подзаголовки разбивают материал. Видно, как работает типографика — предлоги не
висят, тире длинные.
*Слабое:* листинг из трёх карточек в сетке на три колонки выглядит пустовато.
Станет нормально к пятой-шестой статье.

**Контакты.** Три способа связи плашками, форма на два режима.
*Слабое:* «Заявка» и «Заказать звонок» переключаются кнопками с `role="tab"`,
но без обработки стрелок клавиатуры — переключение только Tab и Enter.

**Мобильная версия.** Одна колонка, бургер, полоса связи снизу выезжает после
первого экрана, цифры в 2×2, прайс карточками, калькулятор рабочий.
*Слабое:* заголовок главной занимает четыре строки из шести видимых на 390 px —
первый экран получается плотным.

**404.** Все разделы плашками плюс форма — страница работает, а не извиняется.

---

## Что нужно сделать до публикации

### Обязательно

1. **Реквизиты юрлица** — `src/data/site.ts`: `legalName`, `inn`, `ogrn`,
   `streetAddress`, `postalCode`. Без ИНН политика конфиденциальности не
   соответствует ч. 2 ст. 18.1 152-ФЗ. Пока поля пусты, в футере видна красная
   пометка, а на странице политики — блок с перечнем.
2. **Контакты** — `phone`, `phoneHuman`, `email`, `telegram`, `telegramUrl`.
   Сейчас стоят заглушки из README прототипа: `+7 495 123-45-67`,
   `info@profid.ru`, `@profid_pto`.
3. **Прайс** — `src/data/prices.ts`, 28 позиций. Собраны по рыночным ориентирам
   из ТЗ, владельцем не подтверждены. **Самый рискованный блок сайта.**
4. **Приёмник форм** — `formEndpoint` в `site.ts`. Требования — в `README.md`.
5. **Домен**, если он не `profid.ru`: `site.ts`, `astro.config.mjs`,
   `public/robots.txt`.

### Проверить или заменить

6. **Четыре цифры первого экрана** — `stats` в `site.ts`: «12+ лет в ПТО»,
   «340 томов сдано», «9 из 10 принимают сразу», «60 мин на ответ». Заявленные
   ориентиры, не подтверждённая статистика. Стоят на первом экране — расхождение
   с реальностью здесь дороже всего.
7. **Годы работ по объектам** — `src/content/objects/*.md`, поле `year`, во всех
   четырёх стоит `TODO`.
8. **Пятая карточка объекта** — не создана намеренно, придумывать объект нельзя.
9. **Счётчик Метрики** — `analytics.yandexMetrika`, плюс настроить 8 целей
   (список в `README.md`).

### После выкладки

10. Перемерить скорость на PageSpeed Insights — локальные числа не заменяют
    боевые.
11. Прогнать микроразметку через валидатор Яндекса и Rich Results Test.
12. Прогнать HTML через validator.w3.org.
13. Отдать `sitemap-index.xml` в Вебмастер и Search Console.
14. Проверить отображение в Яндекс.Браузере и Safari — из окружения доступен
    только Chromium, проверка в четырёх браузерах (п. 7 критериев приёмки ТЗ)
    **не выполнена**.

---

## Чего в этой сдаче нет

Перечислено прямо, без попытки замаскировать.

| Что | Почему | Что делать |
|---|---|---|
| Фотографии объектов | Своих фото нет, генератора нет, Unsplash/Pexels/Pixabay закрыты сетевой политикой окружения | Заменить графику на реальные снимки, порядок в `README.md` |
| Фоновое видео | Coverr, Pexels Video, Mixkit недоступны; плохое видео хуже отсутствия | Опционально по ТЗ, можно не делать |
| 7 из 10 статей блога | Написаны три приоритетных из раздела 6 ТЗ | План следующих семи — на `/blog`, порядок по частотности |
| Пятая карточка объекта | Придуманный объект в портфолио недопустим | Владелец даёт объект — карточка добавляется файлом |
| Гео-страницы (кластер Г) | Вторая очередь по ТЗ, требуют по 1 500 знаков уникального текста на город | Каркас готов, `serviceArea` в `site.ts` |
| Проверка в Safari и Яндекс.Браузере | В окружении только Chromium | Проверить вручную после выкладки |
| Валидаторы Яндекса, Google и W3C | Сеть окружения закрыта | Прогнать после выкладки |
| Сверка ядра с Вордстатом | Нет доступа к Вордстату | Прогнать ядро и вычистить нулевую частотность (п. 2.5 ТЗ) |

---

## Что было найдено и исправлено по ходу

Не «всё сразу получилось». Список реальных дефектов, найденных проверками:

1. Блоки появления не показывались при быстром скролле — 18 из 47 оставались
   невидимыми. Добавлена подметающая проверка в обработчик скролла.
2. Битый `<rect width="150' ">` в чертеже комплекта — ломал разбор SVG.
3. Сетка чертежей выходила за `viewBox` и давала горизонтальный скролл 7 px.
4. Селектор `.hero-stat span` захватывал `span` счётчика внутри `dt` — цифра и
   суффикс разъезжались на две строки («12» и «+»).
5. Штамп приёмки на плане наезжал на нижнюю размерную цепочку; на титульном
   листе — на две последние строки описи.
6. `--tx-l-dim` давал 3,9:1 на тёмной светлой поверхности вместо 4,5:1.
7. Ссылка-логотип и ползунки калькулятора были ниже 44 px.
8. Доступное имя ссылки-логотипа не содержало весь видимый текст знака.
9. На `/ceny` иерархия шла `h1 → h3` с пропуском уровня.
10. Бесплатная услуга показывалась как «от 0 ₽».
11. Шесть описаний вышли за 140–160 знаков.
12. Двенадцать идентификаторов заголовков начинались с цифры.
13. Ошибка в самой проверке контраста: полупрозрачный фон читался как
    непрозрачный и давал ложные срабатывания.
14. Заглушка `TODO` из данных выводилась в карточках портфолио на главной.
15. Штриховки сечений не отображались: правило `fill: none` в CSS перебивало
    презентационный атрибут `fill="url(#…)"` — атрибут слабее любого правила.
16. `stroke-dasharray` отрисовки был короче периметра рамки, и длинные контуры
    оставались разорванными навсегда.
17. Первый размер в цепочке плана не помещался под своим отрезком.
